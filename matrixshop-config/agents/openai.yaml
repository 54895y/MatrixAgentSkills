interface:
  display_name: "MatrixShop Config"
  short_description: "Generate and edit MatrixShop YAML configs from current module, refresh, and compatibility rules"
  default_prompt: "Use $matrixshop-config to generate or modify MatrixShop YAML files based on the current MatrixShop config model, SystemShop goods and refresh structure, bindings rules, economy priority, and verified compatibility facts."
